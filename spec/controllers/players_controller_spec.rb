require 'spec_helper'

describe PlayersController do
  describe "daily_projections" do
    it "loads all players from today's game"
    it "does not load any players if there is no game today"
    it "calculates the league multiplier for the right position"
    it "calculates the opponent multiplier for previous opponents of the current opponent"

  end
end
